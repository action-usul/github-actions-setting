name: envirement

on:
  push

jobs:
  get-env:
    runs-on: ubuntu-latest
    steps:
      - name: check env & secret
        run: |
          echo ${{ vars.level }} # 변수
          echo ${{ secrets.key }} # 비밀번호

